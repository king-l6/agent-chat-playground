# 刘浩然 · AI Agent 应用开发求职补充手册

> 来源：BOSS 直聘 150 条「Agent / 前端 @ 上海」JD 分析 + 现有简历对照。  
> 用途：学习清单、简历改写参照；也可作为本项目后续 RAG 知识库文档。  
> 决策（2026-09-12）：先做完 4 周「可下载交付工作台」再改简历标题投递，不走两周速投。

---

## 1. 市场在要什么（150 条 JD 摘要）

| 维度 | 结论 |
|------|------|
| 经验 | 3–5 年为主，其次 5–10 年、1–3 年 |
| 学历 | 几乎一律本科 |
| 薪资中位 | 大致 15–30K；专家/大厂可到 30–70K+ |
| 标题高频 | Agent / AI Agent、前端、全栈 |
| 技术主线 | **React + TypeScript** 强于 Vue；常带 Node 全栈 |

### 共性职责（不是传统 CRUD）

1. Agent 产品前端：对话 UI、工作流/编排画布、插件工具、任务可视化  
2. AI 交互工程：SSE/WebSocket 流式、长对话性能、工具调用可视化、思维链/溯源  
3. 大模型对接：API、RAG、memory、function calling / MCP  
4. 部分岗要前端架构/基建；少数偏 Three.js / 数字孪生  

### 技术栈信号（按命中岗位数大致排序）

- 必碰：Agent 概念、React、TS/JS、工作流/画布、大模型 API、RAG/工具调用  
- 强加分：Vite/Webpack、开源作品、Node、流式 SSE/WebSocket  
- 选学：Vue、Python/Go、CI/CD、Three.js（非主路径）

**一句话目标画像（前端岗，MVP 已齐）**：能独立做出「可演示的 AI Agent 前端产品」（对话 + 工具调用 + RAG/编排）。  
**一句话目标画像（应用岗，四周后）**：能独立做出「别人能装的本地 Agent 工作台」——多角色按产研链路交接，内核仍是现有 SSE / tool / Skill / RAG / 画布。

---

## 2. 我当前具备 vs 缺口

### 已具备（简历可写实）

- 约 4 年前端（德臻医疗信息化 + 易单多端业务）  
- **Vue3 + TypeScript + Vite + Vben Admin** 中后台、复杂表单、权限、打印/填报  
- React（Redux / Router / Ant Design）项目经验  
- 多端：PC、小程序 / uni-app  
- 了解 Node / Express / MySQL；有技术博客习惯  

### 前端岗缺口（本仓库 MVP 已补）

| 缺口 | 状态 |
|------|------|
| 流式对话 SSE/WebSocket | 已落地 |
| Agent / tool calling 产品形态 | 已落地（单 Agent 循环） |
| 对接大模型 API | 已落地（live + mock） |
| RAG 检索 + 引用展示 | 已落地（黄金集 75%→100%） |
| Node 轻量服务闭环 | 已落地 |
| 工作流画布 | 已落地（拓扑序 + 分流） |
| 对口开源作品 | 仓库已有，投递前确认已 push |

### 应用岗缺口（四周竖切要补，未完成前不要写进简历）

| 缺口 | 说明 |
|------|------|
| 可分发桌面载体 | Electron 安装包；别人不用 `npm run dev` |
| 环境动作 | 选本地仓库、受限读写文件、读 git diff |
| 真多 Agent | 四个角色 + 类型化产物交接，不是两个气泡互聊 |
| 产研竖切 | 需求 → 实现 → 评审 → 交付物；人可中断 |

> 原则：**不虚构经历**。现有 playground 只证明 Agent 前端；应用岗等第 9 节四周验收过关再改摘要。

---

## 3. 第一轮已齐：本项目 MVP（Agent 前端）

仓库：`agent-chat-playground`  
目标：**能演示**，不是做 Dify。应用岗第二轮见第 9 节，不要在这一节上继续堆功能。

### 必须有（Week 1–2）

1. Chat UI：消息列表、气泡、loading、错误/停止  
2. SSE 流式：逐段出现  
3. Tool calling 卡片：调用中 → 结果 → 继续回答  

### 强烈建议（Week 3）

4. 最小 RAG：上传文档 → 检索 → 回答带引用  

### 已补上（对标平台岗）

- 可视化画布：拓扑序、条件分流、本地存图；不是 Dify  

### 技术选型（本仓库）

- 前端：React + TypeScript + Vite  
- 后端：Express + OpenAI 兼容 API（DeepSeek 等）  
- 无 Key：mock 模式仍可演示流式 + 工具卡片  

### 本地工具（演示用）

- `get_current_time`  
- `calculator`  
- `search_notes`（简易知识库检索）  

---

## 4. 周计划 Checklist

### 第一轮（Agent 前端 MVP）— 已齐

- [x] SSE 流式 + 停止  
- [x] Tool calling 卡片状态机  
- [x] RAG 引用 + 黄金集 eval  
- [x] 编排画布 + Skill  
- [ ] 推到 GitHub（远端 `king-l6/agent-chat-playground`，投递前确认最新）  
- [ ] 录 90 秒 Demo（步骤见仓库 README；四周后换成安装包演示）

### 第二轮（Agent 应用，4 周后再投）

详见第 9 节。未验收前，意向仍可对外说「Agent 前端」，不要提前改成「应用开发」。

---

## 5. 每天优先学什么（边做边查）

1. SSE 流式前端（收益最大）  
2. LLM API + tool calling 协议  
3. React 复杂对话态（不可变更新、`AbortController`）  
4. Node 最小服务（转发 stream、读 env）  
5. RAG 概念：切分 / TopK / 引用（Week3）  
6. 编排画布 / Skill 已接到对话循环；MCP / LangChain 仍后置  
7. Electron 主进程 IPC + 工作区沙箱（第二轮 Week A）  
8. 多 Agent 交接协议：产物进黑板，不靠自由文本互聊（第二轮 Week B）  

---

## 6. 简历改写要点（对照 JD）

### 要突出

- 四周验收前：React / TS、流式、Agent、RAG、工具调用、轻量 Node、可演示仓库  
- 四周验收后：可安装桌面工作台、工作区文件/git、多角色交接、产研竖切；SSE/RAG/画布改口为「内核」  
- 结果与指标（Recall@3、一次真实自用交付、安装包能打开）

### 少写或后置

- 纯业务 CRUD、只会切图  
- 空洞的「熟悉 AI」  
- 模型训练/微调（岗位要的是应用层）  
- 没做完的 Electron / 多 Agent / 「AI 编码平台」（未验收就写 = 虚构）

### 个人项目模板 A（现在就能贴，投前端岗用）

**Agent Chat Playground**｜个人项目｜https://github.com/king-l6/agent-chat-playground  

- 基于 React + TypeScript 实现 SSE 流式对话：边收边渲染、可停止、工具卡片（调用中 / 结果 / 失败）  
- Node 对接 OpenAI 兼容 API，多轮 tool calling 后再汇总；无 Key 时 mock 仍可演示全流程  
- 本地 RAG（BGE 向量 + 关键词融合 + 重排），8 题黄金集 Recall@3 从 75% 提到 100%，回答带引用  
- 编排画布：DAG 拓扑序 + 条件分流（规则编译成 pipeline）；图存拓扑、不存运行结果

### 个人项目模板 B（第 9 节四周验收后整段替换 A，投应用岗用）

**本地交付工作台**｜个人项目｜https://github.com/king-l6/agent-chat-playground  

- 用 Electron 做出可安装的本地 Agent 工作台：选仓库后受限读写文件，并读取 git status / diff  
- 按产研角色拆成需求 / 实现 / 评审 / 交付四个 Agent，产物进共享黑板，交接在 UI 上可见、可中断  
- 对着本仓库跑通一条竖切（例如给 RAG 评测加题），评审能指出具体文件风险，交付物含测试清单与发布说明  
- 内核复用 SSE 流式、tool calling、Skill、本地 RAG（Recall@3 75%→100%）与编排画布；画布用来对比「人画的流程」和「模型自己分工」  

---

## 7. 基本信息（简历抬头用）

- 姓名：刘浩然  
- 意向：四周验收前对外写「前端开发 / AI Agent 前端」；验收后改为「前端开发 / AI Agent 应用开发」  
- 城市：上海  
- 电话：15544009307  
- 邮箱：liuhaoran284@163.com  
- 博客：https://king-l6.github.io  

### 工作与教育（事实，勿夸大）

- 德臻（上海）信息科技｜前端｜2023.04 – 至今（华山医院等工作台 / MDM / 上报系统）  
- 郑州易单信息科技｜前端｜2021.07 – 2023.03  
- 安阳学院｜本科｜计算机科学与技术（师范）｜2018 – 2022  

---

## 8. 本仓库怎么跑

```bash
cd agent-chat-playground
cp .env.example .env   # 可选填 OPENAI_API_KEY
npm install
npm run dev
```

- 前端：http://localhost:5176/  
- 后端：http://127.0.0.1:8790  

试着问：现在几点了？帮我算 123*456；这个项目的技术栈是什么？

---

## 9. 第二轮：4 周后再投（已锁定）

产品就一件：**Electron 本地交付工作台**。多 Agent 是干活的人，产研流程是他们为什么配合，现有 playground 是内核。不新开仓库，不做成 Cursor / Dify。

竖切只这一条：

```
一句话需求
  → 需求 Agent  → 结构化 PRD
  → 实现 Agent  → 白名单内补丁 / 改文件
  → 评审 Agent  → 带文件路径的风险清单
  → 交付 Agent  → 测试清单 + 发布说明
```

四个角色不靠互聊。每个角色的产物是下一个的输入。人可以在任一步打断。

刻意不做：云端多租户、Windows 安装包、N 个 Agent 自由辩论、再堆画布节点、通用 IDE。

### Week A · 能装，能碰文件

- [ ] **A1** 用 electron-vite（或 electron-builder 包现有 Vite）接入桌面壳，`npm run dev` 网页模式必须还能跑  
      文件：`package.json`、`electron/main.ts`、`electron/preload.ts`、`vite.config.ts`  
      验收：`npm run electron:dev` 打开本应用窗口，不是系统浏览器
- [ ] **A2** 主进程 IPC：选目录、列目录、读文件、写文件；路径必须落在用户选中的根目录内  
      文件：`electron/workspace.ts`、`server/src/tools.ts`（加 workspace_* 工具）  
      验收：选中本仓库，Agent 能读到 `README.md`；写 `../` 被拒绝
- [ ] **A3** `git_status` / `git_diff` 工具（只读，cwd = 工作区根）  
      文件：`server/src/tools.ts`  
      验收：问「当前改了什么」，先出工具卡片，再根据 diff 回答
- [ ] **A4** 前端工作区条：显示当前根目录、重选、未选择时禁用文件类工具  
      文件：`src/App.tsx` 或新 `src/components/WorkspaceBar.tsx`  
      验收：网页模式提示「桌面版才能选仓库」；Electron 模式能选
- [ ] **A5** `electron-builder` 打出 macOS zip/dmg，挂到 GitHub Releases  
      文件：`electron-builder.yml`、`.github/workflows/release.yml`（可先本地 `npm run dist`）  
      验收：另一台机器或不装 Node 的账号，下载打开能进对话页

**Week A 出门标准**：同事不跑 `npm run dev`，装上能打开；选中本仓库，Agent 能读 README。

### Week B · 四个角色 + 可见交接

- [ ] **B1** 黑板类型：`prd` / `tasks` / `patch` / `review` / `release_notes`（JSON，不要自由文本当协议）  
      文件：`server/src/delivery/types.ts`  
      验收：用 TypeScript 能描述完整一次 run 的状态，缺产物时下一角色不能开跑
- [ ] **B2** Supervisor 状态机：按序唤起角色，写入黑板，推 SSE  
      文件：`server/src/delivery/runtime.ts`（不要把四种人设塞进现有 `agent.ts` 的 system prompt）  
      验收：mock 无 Key 也能按序吐出四段产物
- [ ] **B3** 四个 specialist：各自工具白名单（需求只读写知识/黑板；实现才能 workspace_write；评审只读；交付读 diff + 黑板）  
      文件：`server/src/delivery/roles.ts`、`server/skills/delivery-*/SKILL.md`  
      验收：实现 Agent 调不了「随便写任意路径」；评审 Agent 调不了 write
- [ ] **B4** SSE 新事件：`role_start` / `role_done` / `artifact`；前端角色条 + 产物面板  
      文件：`server/src/types.ts`、`src/api/chat.ts`、`src/components/RoleStrip.tsx`  
      验收：输入「给 RAG 评测加一道题」，能看见四段产物依次出现，后一段引用前一段的字段，不是各说各话
- [ ] **B5** 人可中断：某一角色 `done` 后可「改产物再继续」或「跳过」  
      文件：`src/components/ArtifactPanel.tsx`、对应 POST  
      验收：改 PRD 标题后再跑实现，实现吃到的是改过的版本

**Week B 出门标准**：一条需求跑完，UI 上能指着说「这是交接，不是两个模型在聊天」。

### Week C · 对着本仓库真跑一回

- [ ] **C1** 实现 Agent 写入白名单：默认只允许 `server/src/eval.ts`、`server/src/eval-cases.ts`（没有就新建后者）  
      验收：它改不了 `server/src/agent.ts`
- [ ] **C2** 自用一次：需求 =「给 RAG 评测加一道题」；跑通后 `npm run eval:rag` 不能坏掉原有 8 题  
      验收：你能指着 git diff 说「这是 Agent 交的活，我 review 过」
- [ ] **C3** 评审产物必须带 `path` + 风险说明；缺路径算失败（mock 也要有假路径，live 必须来自 diff）  
      验收：面板上能点到具体文件
- [ ] **C4** 交付物从黑板 + `git_diff` 生成测试清单和发布说明，禁止空话  
      验收：发布说明里出现本轮改动的文件名

**Week C 出门标准**：你自己用它交过一次活，不是只演示过。

### Week D · 作品化再改简历

- [ ] **D1** 新 90 秒 Demo：打开安装包 → 选本仓库 → 跑完一条交付 → 指评审卡住的地方（替换 README 旧演示表）  
- [ ] **D2** README「有意没做」补上：不是 Cursor、不是多租户、Windows 包没有、实现只改白名单  
- [ ] **D3** 把本节 checklist 勾完；简历整段换成第 6 节模板 B；意向改为「AI Agent 应用开发」  
- [ ] **D4** 口述准备：Electron 主进程为什么碰文件 / 交接协议长什么样 / 人画的画布 vs 模型分工 / 生产还缺什么

**Week D 出门标准**：安装包 + 仓库 + 口述齐了，再投应用岗。

### 开写顺序（一次只做一项，依赖写在后面）

| 序号 | 任务 | 依赖 | 对面试官多一句话 |
|------|------|------|------------------|
| 1 | A1 桌面壳，网页 dev 不坏 | — | 这是可分发软件，不是演示页 |
| 2 | A2 工作区沙箱 IPC + 工具 | 1 | Agent 能动本地环境 |
| 3 | A4 工作区条 UI | 2 | 人选根目录，模型不能自己乱爬盘 |
| 4 | A3 git 只读工具 | 2 | 后续评审/交付有事实来源 |
| 5 | A5 打 macOS 包 | 1 | 别人能下载 |
| 6 | B1 黑板类型 | — | 交接是协议，不是聊天 |
| 7 | B2 Supervisor | 6 | 控制面在我这，不在模型心情 |
| 8 | B3 四角色白名单 | 2, 7 | 谁能写文件是硬约束 |
| 9 | B4 角色条 + 产物面板 | 7 | 配合过程可见 |
| 10 | B5 人改产物再继续 | 9 | 人在环里 |
| 11 | C1–C4 对本仓库自用 | 8, 4 | 我用它交过活 |
| 12 | D1–D4 演示/README/简历 | 5, 11 | 写进简历的都能打开 |

从 **A1** 开写。做完一项勾一项，不要先搭「平台框架」。

---

*文档随学习进度自行勾选更新；投递前再核对一遍「写进简历的」是否都有可演示证据。*
