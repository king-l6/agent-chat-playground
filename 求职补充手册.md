# 刘浩然 · AI Agent 前端求职补充手册

> 来源：BOSS 直聘 150 条「Agent / 前端 @ 上海」JD 分析 + 现有简历对照。  
> 用途：学习清单、简历改写参照；也可作为本项目后续 RAG 知识库文档。

---

## 1. 市场在要什么（150 条 JD 摘要）

| 维度 | 结论 |
|------|------|
| 经验 | 3–5 年为主，其次 5–10 年、1–3 年 |
| 学历 | 几乎一律本科 |
| 薪资中位 | 大致 15–30K；专家/大厂可到 30–70K+ |
| 标题高频 | Agent / AI Agent、前端、全栈 |
| 技术主线 | **React + TypeScript** 强于 Vue；常带 Node 全栈 |

### 共性职责（不是传统 CRUD）

1. Agent 产品前端：对话 UI、工作流/编排画布、插件工具、任务可视化  
2. AI 交互工程：SSE/WebSocket 流式、长对话性能、工具调用可视化、思维链/溯源  
3. 大模型对接：API、RAG、memory、function calling / MCP  
4. 部分岗要前端架构/基建；少数偏 Three.js / 数字孪生  

### 技术栈信号（按命中岗位数大致排序）

- 必碰：Agent 概念、React、TS/JS、工作流/画布、大模型 API、RAG/工具调用  
- 强加分：Vite/Webpack、开源作品、Node、流式 SSE/WebSocket  
- 选学：Vue、Python/Go、CI/CD、Three.js（非主路径）

**一句话目标画像**：能独立做出「可演示的 AI Agent 前端产品」（对话 + 工具调用 + 可选 RAG/编排）。

---

## 2. 我当前具备 vs 缺口

### 已具备（简历可写实）

- 约 4 年前端（德臻医疗信息化 + 易单多端业务）  
- **Vue3 + TypeScript + Vite + Vben Admin** 中后台、复杂表单、权限、打印/填报  
- React（Redux / Router / Ant Design）项目经验  
- 多端：PC、小程序 / uni-app  
- 了解 Node / Express / MySQL；有技术博客习惯  

### 明显缺口（投 Agent 岗前要补）

| 缺口 | 说明 |
|------|------|
| 流式对话 SSE/WebSocket | JD 高频，面试必问 |
| Agent / tool calling 产品形态 | 岗位核心差异点 |
| 对接大模型 API | 「会 AI」的最低证据 |
| RAG 检索 + 引用展示 | 约 1/3+ JD 会写 |
| Node 轻量服务闭环 | 全栈向岗常要 |
| 工作流画布 | 平台/专家岗才硬要，MVP 可后置 |
| 对口开源作品 | 约 40% JD 看开源/作品 |

> 原则：**不虚构经历**。先用本仓库做出可演示 MVP，再把摘要从「在学」改成「已落地」。

---

## 3. 最快补法：本项目 MVP（2–4 周）

仓库：`agent-chat-playground`  
目标：**能演示**，不是做 Dify。

### 必须有（Week 1–2）

1. Chat UI：消息列表、气泡、loading、错误/停止  
2. SSE 流式：逐段出现  
3. Tool calling 卡片：调用中 → 结果 → 继续回答  

### 强烈建议（Week 3）

4. 最小 RAG：上传文档 → 检索 → 回答带引用  

### 先不做

- 可视化画布、多 Agent、权限体系、模型微调、自建向量集群  

### 技术选型（本仓库）

- 前端：React + TypeScript + Vite  
- 后端：Express + OpenAI 兼容 API（DeepSeek 等）  
- 无 Key：mock 模式仍可演示流式 + 工具卡片  

### 本地工具（演示用）

- `get_current_time`  
- `calculator`  
- `search_notes`（简易知识库检索）  

---

## 4. 周计划 Checklist

### Week 1：流式 Chat

- [ ] 读懂 `src/api/chat.ts`（ReadableStream 读 SSE）  
- [ ] 读懂消息结构：`role / content / status / tools`  
- [ ] 跑通 mock：边收边渲染、停止生成  
- [ ] （可选）配置 `.env` 的 `OPENAI_API_KEY` 走 live  

**验收**：输入一句话，字一个个出来。

### Week 2：Tool calling

- [ ] 读懂 `server/src/agent.ts`（stream → tool_calls → 执行 → 再问模型）  
- [ ] 读懂前端工具状态机：`running / done / error`  
- [ ] 自己加第 4 个本地工具并出卡片  
- [ ] 能口述：和纯 Chatbot 的区别  

**验收**：问时间/计算，先出工具卡片再出最终答案。

### Week 3：RAG + 作品化

- [ ] 把本文档 / 简历摘要接入可上传检索（或扩展 `search_notes`）  
- [ ] 回答带引用片段  
- [ ] README 架构说明 + 30–60 秒 Demo 录屏  
- [ ] 推到 GitHub 公开仓库  

**验收**：问「我缺什么 / 怎么学」，能基于文档回答。

### Week 4：简历与面试对齐

- [ ] 简历加「个人项目」4 条 bullet  
- [ ] 摘要改为「已独立落地 SSE + tool calling …」  
- [ ] 准备口述：SSE 怎么接、tool 状态怎么设计、生产还缺什么  

若只有 **2 周**：做完 Week1+Week2 即可投递一批评测岗。

---

## 5. 每天优先学什么（边做边查）

1. SSE 流式前端（收益最大）  
2. LLM API + tool calling 协议  
3. React 复杂对话态（不可变更新、`AbortController`）  
4. Node 最小服务（转发 stream、读 env）  
5. RAG 概念：切分 / TopK / 引用（Week3）  
6. 画布 / MCP / LangChain 深水区 → 投平台专家岗再补  

---

## 6. 简历改写要点（对照 JD）

### 要突出

- React / TS、流式、Agent、RAG、工具调用、轻量 Node  
- 可演示链接（本仓库）  
- 结果与指标（卡顿、首 token、交付反馈）  

### 少写或后置

- 纯业务 CRUD、只会切图  
- 空洞的「熟悉 AI」  
- 模型训练/微调（岗位要的是应用层）  

### 个人项目可写模板（落地后粘贴）

**Agent Chat Playground**｜个人项目｜时间  

- 基于 React + TypeScript 实现流式对话 UI，SSE 边收边渲染  
- 实现 tool calling 状态机与工具卡片（调用中 / 结果 / 失败）  
- Node 服务对接 OpenAI 兼容 API，支持多轮工具调用再汇总回答  
- 内置简易检索，演示「检索 → 引用回答」链路；（后续）文档上传 RAG  

---

## 7. 基本信息（简历抬头用）

- 姓名：刘浩然  
- 意向：前端开发 / AI Agent 前端  
- 城市：上海  
- 电话：15544009307  
- 邮箱：liuhaoran284@163.com  
- 博客：https://king-l6.github.io  

### 工作与教育（事实，勿夸大）

- 德臻（上海）信息科技｜前端｜2023.04 – 至今（华山医院等工作台 / MDM / 上报系统）  
- 郑州易单信息科技｜前端｜2021.07 – 2023.03  
- 安阳学院｜本科｜计算机科学与技术（师范）｜2018 – 2022  

---

## 8. 本仓库怎么跑

```bash
cd agent-chat-playground
cp .env.example .env   # 可选填 OPENAI_API_KEY
npm install
npm run dev
```

- 前端：http://localhost:5176/  
- 后端：http://127.0.0.1:8790  

试着问：现在几点了？帮我算 123*456；这个项目的技术栈是什么？

---

## 9. 下一步（写完 MVP 之后）

1. 真·分块 RAG + 引用角标  
2. 部署（前端 Vercel + 后端任意 Node）  
3. 简易工作流画布（对标平台岗）  
4. 开源 README 写清架构图与面试话术  

---

*文档随学习进度自行勾选更新；投递前再核对一遍「写进简历的」是否都有可演示证据。*
